{
  "build": {
    "builder": "python"
  },
  "deploy": {
    "command": "gunicorn telegram-bot:app --bind 0.0.0.0:$PORT",
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}